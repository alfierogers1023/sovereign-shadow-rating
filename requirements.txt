# Phase 0 needs only these; later phases add the modelling stack.
wbgapi>=1.0.12
fredapi>=0.5.2
requests>=2.31         # IMF DataMapper API fallback for WEO
certifi>=2024.0        # CA bundle for urllib (fredapi) on python.org macOS builds
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # parquet caching

# Phase 3 -- Model B (ordered logit + gradient boosting):
scikit-learn>=1.3
statsmodels>=0.14
mord>=0.6              # ordered logistic regression
xgboost>=2.0

# Phases 5-6 (uncomment as you reach them):
# plotly>=5.18
